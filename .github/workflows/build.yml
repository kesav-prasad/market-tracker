name: Build Electron App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Check out Git repository
      uses: actions/checkout@v4

    - name: Install Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Clean lockfiles
      run: |
        Remove-Item -ErrorAction SilentlyContinue package-lock.json
        Remove-Item -ErrorAction SilentlyContinue frontend\package-lock.json
        Remove-Item -ErrorAction SilentlyContinue backend\package-lock.json

    - name: Install dependencies
      run: npm install --no-package-lock --legacy-peer-deps

    - name: Install frontend dependencies
      working-directory: ./frontend
      run: npm install --no-package-lock --legacy-peer-deps

    - name: Install backend dependencies
      working-directory: ./backend
      run: npm install --no-package-lock --ignore-scripts --legacy-peer-deps || (Get-ChildItem -Path "$env:LocalAppData\npm-cache\_logs\*.log" | Sort-Object LastWriteTime | Select-Object -Last 1 | Get-Content -Tail 50; exit 1)

    - name: Generate Prisma Client
      working-directory: ./backend
      run: npx prisma generate

    - name: Build and Package Electron App
      run: npm run dist

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MarketTracker-Setup
        path: dist/*.exe

    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
      with:
        files: dist/*.exe
        tag_name: latest
        name: Latest Release
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
